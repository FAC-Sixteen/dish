after_success:
  - bash <(curl -s https://codecov.io/bash) -e TRAVIS_NODE_VERSION
install:
  - npm install
  - npm install -g codecov
language: node_js
node_js:
  - node
script:
  - npm test
  - npm run coverage

